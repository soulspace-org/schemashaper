(ns org.soulspace.schemashaper.adapter.cli-test
  (:require [clojure.test :refer :all]
            [org.soulspace.schemashaper.adapter.cli :refer :all]))

(deftest compile-test
  (testing "Compile test"
    (is (= 1 1))))
