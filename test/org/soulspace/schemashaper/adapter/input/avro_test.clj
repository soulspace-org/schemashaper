(ns org.soulspace.schemashaper.adapter.input.avro-test
  (:require [clojure.test :refer :all]
            [org.soulspace.schemashaper.adapter.input.avro :refer :all]))

(deftest compile-test
  (testing "Compile test"
    (is (= 1 1))))
