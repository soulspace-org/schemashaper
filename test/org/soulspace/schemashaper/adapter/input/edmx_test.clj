(ns org.soulspace.schemashaper.adapter.input.edmx-test
  (:require [clojure.test :refer :all]
            [org.soulspace.schemashaper.adapter.input.edmx :refer :all]))

(deftest compile-test
  (testing "Compile test"
    (is (= 1 1))))
