(ns org.soulspace.schemashaper.adapter.input.overarch-test
  (:require [clojure.test :refer :all]
            [org.soulspace.schemashaper.adapter.input.overarch :refer :all]))

(deftest compile-test
  (testing "Compile test"
    (is (= 1 1))))
