(ns org.soulspace.schemashaper.adapter.output.avro-test
  (:require [clojure.test :refer :all]
            [org.soulspace.schemashaper.adapter.output.avro :refer :all]))

(deftest compile-test
  (testing "Compile test"
    (is (= 1 1))))
