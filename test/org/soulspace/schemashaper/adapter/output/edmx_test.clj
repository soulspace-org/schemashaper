(ns org.soulspace.schemashaper.adapter.output.edmx-test
  (:require [clojure.test :refer :all]
            [org.soulspace.schemashaper.adapter.output.edmx :refer :all]))

(deftest compile-test
  (testing "Compile test"
    (is (= 1 1))))
