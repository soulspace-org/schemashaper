(ns org.soulspace.schemashaper.adapter.output.overarch-test
  (:require [clojure.test :refer :all]
            [org.soulspace.schemashaper.adapter.output.overarch :refer :all]))

(deftest compile-test
  (testing "Compile test"
    (is (= 1 1))))
