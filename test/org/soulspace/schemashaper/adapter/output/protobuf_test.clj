(ns org.soulspace.schemashaper.adapter.output.protobuf-test
  (:require [clojure.test :refer :all]
            [org.soulspace.schemashaper.adapter.output.protobuf :refer :all]))

(deftest compile-test
  (testing "Compile test"
    (is (= 1 1))))
