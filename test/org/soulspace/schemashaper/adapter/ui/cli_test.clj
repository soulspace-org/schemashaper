(ns org.soulspace.schemashaper.adapter.ui.cli-test
  (:require [clojure.test :refer :all]
            [org.soulspace.schemashaper.adapter.ui.cli :refer :all]))

(deftest compile-test
  (testing "Compile test"
    (is (= 1 1))))
